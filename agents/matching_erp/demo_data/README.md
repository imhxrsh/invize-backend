# Demo ERP data (dummy only)

This folder contains **template/dummy** PO, GRN, and vendor data for end-to-end **demo** of the Matching & ERP agent. **No real ERP** is connected.

- **Tally** and **SAP** connectors will be built later and plugged in via the same adapter interface.
- `demo_erp.json` is loaded by the **demo adapter** when `ERP_TYPE=demo`.
